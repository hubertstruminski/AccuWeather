import { StyleSheet } from 'react-native';
import startScreenStyles from '../startScreen/startScreenStyle';

export default StyleSheet.create({
    container: {
        ...startScreenStyles.container,
    },
});